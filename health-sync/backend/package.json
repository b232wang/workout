{
  "name": "health-ingest",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Receive Apple Health workouts from iPhone, store in SQLite, serve to Mac.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "express": "^4.19.0"
  }
}
